---
const y_offset = 50;
const { item_size, gap } = Astro.props;
const size = 2 * item_size + 2 * gap;
const side = size / Math.sqrt(2);
---

<div class="cursor-rail">
  <div id="cursor"></div>
</div>

<style>
  .cursor-rail {
    width: 100%;
    height: 300px;
  }
  #cursor {
    width: 100px;
    height: 100px;
    border-right: 2px solid #fffff8;
    border-bottom: 2px solid #fffff8;
    transform: rotate(45deg);
    position: absolute;
  }
</style>
